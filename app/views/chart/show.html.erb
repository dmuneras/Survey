<% title 'Gráficos' %>

<div class= "result">
 <h1>Resultados</h1>
  <div id="graph">
    <%= @graph %>
  </div>
</div>


<ol>
  <% for aspect in @aspects do %>
  <li><%= aspect.name %></li>
  <% end %>
</ol>

<br />

<% if @next_chart %>
<%= link_to "Siguiente resultado", {:action => :show, :id => @next_chart}, :class => 'next-chart' %>
<% end %>
<% if @prev_chart %>
<%= link_to "Anterior resultado", {:action => :show, :id => @prev_chart}, :class => 'prev-chart' %>
<% end %>
